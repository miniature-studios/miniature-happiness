using System;
using UnityEngine;
using Common;

public class GodModeInsideValidator : IValidator
{
    public bool ValidateCommand(ICommand command)
    {
        throw new NotImplementedException();
    }
}