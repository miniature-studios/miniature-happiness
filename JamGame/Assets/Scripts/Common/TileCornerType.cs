namespace Common
{
    public enum TileCornerType
    {
        inside,
        wall_left,
        wall_right,
        outside_left,
        outside_middle,
        outside_right,
        none
    }
}